---
description: "Add a new CRUD resource / scaffold (with-lingui)."
globs: apps/with-lingui/src/api/**/*.ts,apps/with-lingui/src/mocks/**/*.ts,apps/with-lingui/src/routes/_auth/**/*.tsx,apps/with-lingui/e2e/**/*.spec.ts
alwaysApply: false
---

# with-lingui · Add resource (thin rule)

**Single source of truth:**

`apps/with-lingui/.github/instructions/add-resource.instructions.md`

When adding a resource, follow that recipe (mirror the `users` example). On conflict, the `.instructions.md` file wins.

`@apps/with-lingui/.github/instructions/add-resource.instructions.md`
